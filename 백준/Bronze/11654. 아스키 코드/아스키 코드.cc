#include <iostream>
#include <vector>

using namespace std;


//Main
int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	char getChar;
	cin >> getChar;

	cout << (int)getChar;

	return 0;
}